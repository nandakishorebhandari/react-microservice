// @flow
import * as React from 'react';
import css from './Dashboard.module.css';

const Dashboard = () => (
    <>
        <ul className={css.wrapper}>
            <li className={css.hot}>Dashboard</li>
        </ul>
    </>
);

export default Dashboard;

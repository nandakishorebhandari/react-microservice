// @flow
import * as React from 'react';
import { ReactComponent as ReactLogo } from './react.svg';
import css from './Logo.module.css';
const Logo = () => <ReactLogo className={css.ReactLogo} />;

export default Logo;
